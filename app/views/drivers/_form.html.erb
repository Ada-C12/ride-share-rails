<p>Please provide the following information to save your PASSENGER to our database:</p>

<%= form_with model: @driver, class: 'create-deriver' do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :vin %>
  <%= f.text_field :vin %>

  <%= f.submit "#{action_name} Driver", class: "driver-button" %>

<% end %>

