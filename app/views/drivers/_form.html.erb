<%= form_with model: @driver do |f| %>
<p>Please provide the following information to <%= verb_name %> your driver to our database:</p>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :vin %>
  <%= f.text_field :vin %>

   <%# <%= f.label :mark %>
  <%# <%= f.text_area :mark %>  

   <%# <%= f.label :model %> 
  <%# <%= f.text_area :model %> 

 <%= f.submit "#{action_name} driver", class: "driver-button" %>

<% end %>
