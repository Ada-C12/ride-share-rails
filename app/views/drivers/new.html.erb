<section>
  <h2>Add a New Driver</h2>

  <%= form_with model: @driver, class: "driver-form" do |f| %>
    <h3>Please enter information to add a new driver </h3>

    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.label :vin %>
    <%= f.text_field :vin %>

    <%= f.submit "Add Driver", class: "submit-button" %>
  <% end %>
</section>