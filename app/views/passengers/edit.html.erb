<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>Passenger</title>
  </head>

  <body>
  <body>
    <header>
      <h1>Welcome to Rideshare</h1>
      <nav>
        <ul>
          <%= link_to "Rideshare Rails", root_path %>
          <%= link_to "Passengers", passengers_path %>
          <%= link_to "Drivers", drivers_path %>
        </ul>
      </nav>
    </header>

    <%= form_with model: @passenger do |f| %>
      <p>Please provide the following information to update the passenger to our database:</p>

      <%= f.label :name %>
      <%= f.text_field :name %>

      <%= f.label :phone_number, "Phone Number" %>
      <%= f.text_field :phone_number%>
      <%= f.submit "Update Passenger" %>
    <% end %>
  </body>
</html>