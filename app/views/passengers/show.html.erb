<h2>Details for Passenger: <%= @passenger.name %></h2>

<p>Passenger ID: <%= @passenger.id %></p>
<p>Name: <%= @passenger.name %></p>
<p>Phone Number: <%= @passenger.phone_number %></p>
<p>Total Amount Charged: $<%= number_with_precision(@passenger.total_charged, :precision => 2) %></p>




<h2>Details for Passenger: <%= @passenger.name %></h2>

<ul>
  <li>Contact: <%= @passenger.phone_number %></li>
<li>Total Amount Charged: $<%= number_with_precision(@passenger.total_charged, :precision => 2) %></li>
<li>Passenger's Trips:
    <ul><% @passenger.trips.each do |trip| %>
    <li> <%= link_to trip.id, trip_path(trip.id), class:"link" %> </li>
    <% end %>
</ul>
</li>
<br>
<%= button_to "Edit Passenger".html_safe, edit_passenger_path(@passenger.id), class:"link", method: :get %>
<br>
<%= button_to "Delete Passenger".html_safe, passenger_path(@passenger.id), method: :delete, data: {confirm: "Are you sure you want to delete this passenger?"}, class:"link" %>

<p><%= button_to "Add Trip", passenger_trips_path(@passenger.id), method: :post, class:"link" %></p>

<p><%= button_to "See All Passengers", passengers_path, class:"link", method: :get %></p>
