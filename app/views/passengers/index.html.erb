<div class="index-container">
  <h1>All Passengers<h1>
  <div class="view-all">
    <% @passengers.each do |passenger| %>
      <li><%= link_to passenger.name, passenger_path(passenger) %>
    <% end %>
  </div>
  <br>
   <%= will_paginate @passengers %>
</div>
