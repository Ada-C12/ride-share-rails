<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Passengers</title>
</head>

  <body>
    <header>
      <h1>Welcome to Rideshare</h1>
      <nav>
        <ul>
          <%= link_to "Rideshare Rails", root_path %>
          <%= link_to "Passengers", passengers_path %>
          <%= link_to "Drivers", drivers_path %>
          <%= link_to "Add Driver", new_driver_path %>
          <%= link_to "Add Passenger", new_passenger_path %>
        </ul>
      </nav>
    </header>
    
    <section>
      <h1>Passengers</h1>
      <ol>
        <% @passengers.each do |passenger|  %>
          <li>Name: <%= link_to passenger.name, passenger_path(passenger) %></li>
          <%= button_to 'Delete', passenger_path(passenger),method: :delete, data: {confirm: "Are you sure?"} %>
          <%= button_to 'Edit', edit_passenger_path(passenger.id), method: :get %>
          <hr>
        <% end %>
      </ul>
    </section>
  </body>
</html>