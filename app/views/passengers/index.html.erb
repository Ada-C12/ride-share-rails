<div class="index-container">
  <h1>All Passengers<h1>
     <%= will_paginate @passengers %><br>
  <div class="view-all">
    <% @passengers.each do |passenger| %>
      
      <li>
  
      <%= link_to passenger.name, passenger_path(passenger) %></li>
    <% end %>
  </div>
  <br>

</div>
