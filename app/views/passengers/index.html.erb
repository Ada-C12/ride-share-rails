<%= link_to "Create New Passenger", create_path %>
<ul>
  <h1>All the passengers</h1>
  <% @passengers.each do |passenger| %>
    <li>
    <%= link_to passenger.name, passenger_path(passenger.id) %>
    </li>
  <% end %>
</ul>
