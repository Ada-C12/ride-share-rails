<body>
  <section>
  <table style=width100%>
  <tr>
    <th><h2> List of all Passengers </h2></th>
  </tr>
    <% @passengers.each do |passenger| %>
      <tr>
        <th><%= link_to passenger.name, passenger_path(passenger.id)%></th>
      </tr>
    <% end %>
    </table>
  </section>
</body>