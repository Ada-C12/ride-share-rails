<% if @passenger.errors.any? %>
  <h1>Errors</h1>
  <ul class="errors">
    <% @passenger.errors.each do |column, message| %>
      <li>
        <strong><%= column.capitalize %></strong> <%= message %>
      </li>
    <% end %>
  </ul>
<% end %>

<p>Please provide the following information to <%= verb_name %> a passenger to our database:</p>

<%= form_with model: @passenger, class: 'create-passenger' do |f| %>

<%= f.label :name, "Name: " %>
<%= f.text_field :name %>

<%= f.label :phone_num, "Phone Number: " %>
<%= f.text_field :phone_num %>

<%= f.submit "Save Passenger", class: "passenger-save" %>
<% end %>