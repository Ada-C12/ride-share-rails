    <header>
      <h1>Welcome to D & J Rideshare Company</h1>
      <nav class="navbar">
        <ul class="nav-links">
          <%= link_to "Home", root_path %>
          <%= link_to "Passengers", passengers_path %>
          <%= link_to "Drivers", drivers_path %>
          <%= link_to "Add Driver", new_driver_path %>
          <%= link_to "Add Passenger", new_passenger_path %>
          <% if session[:user_id].nil? %>
            <%= link_to "Login", login_path, method: :get %>
          <% elsif %>
            Logged in as <%= User.find_by(id: session[:user_id]).username %>
            <%= link_to "Logout", logout_path, method: :post%></li>
          <% end %>
        </ul>
    </header>