<h1>Trip Details</h1>

<p><strong>Trip ID:</strong> <%= @trip.id %></p>
<p><strong>Trip Date:</strong> <%= @trip.date %></p>
<p><strong>Trip Cost:</strong> <%= @trip.cost %></p>
<p><strong>Trip Rating:</strong> <%= @trip.rating %></p>
<p><strong>Passenger Name:</strong> <%= link_to @trip.passenger.name, passenger_path(@trip.passenger.id) %></p>
<p><strong>Driver Name:</strong> <%= link_to @trip.driver.name, driver_path(@trip.driver.id) %></p>

<section class="button">
  <%= link_to "Edit Trip", edit_trip_path(@trip) %>
</section> <br>

<section class="button">
  <%= link_to "Delete Trip", trip_path(@trip), method: :delete, data: { confirm: "Are you sure you want to delete this trip?" } %>
</section> <br>