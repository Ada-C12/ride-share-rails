<h1>Trip Details</h1>
<p>Trip Date: <%= @trip.date %></p>
<p>Trip Rating:
<% if @trip.rating == nil %>
  <%= link_to "Add a rating", edit_trip_path(@trip.id) %>
  <% else %>
  <%= @trip.rating %>
<% end%>
</p>
<p>Trip Cost:
<%= @trip.cost %>
</p>
<p>Driver: 
<%= link_to "#{@trip.driver.name}", driver_path(@trip.driver.id) %>
</p>
<p>Passenger:
<%= link_to "#{@trip.passenger.name}", passenger_path(@trip.passenger.id) %>
</p>
<p>
<%= link_to "Delete Trip #{@trip.id}", trip_path, method: :delete, data: { confirm: "Are you sure?" } %>
<%= link_to "Edit Trip #{@trip.id}", edit_trip_path %>
