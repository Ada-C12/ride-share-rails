<h1>Trip Details</h1>

<p>Trip ID: <%= @trip.id %></p>
<p>Trip Date: <%= @trip.date %></p>
<p>Trip Cost: <%= @trip.cost %></p>
<p>Trip Rating: <%= @trip.rating %></p>
<p>Passenger Name: <%= link_to @trip.passenger.name, passenger_path(@trip.passenger.id) %></p>
<p>Driver Name: <%= link_to @trip.driver.name, driver_path(@trip.driver.id) %></p>

<%= link_to "Edit Trip", edit_trip_path(@trip) %><br>
<%= link_to "Delete Trip", trip_path(@trip), method: :delete, data: { confirm: "Are you sure you want to delete this trip?" } %><br>