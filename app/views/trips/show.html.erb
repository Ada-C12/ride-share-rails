<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Trip</title>
</head>

  <body>
    <header>
      <h1>Welcome to Rideshare</h1>
      <nav>
        <ul>
          <%= link_to "Rideshare Rails", root_path %>
          <%= link_to "Passengers", passengers_path %>
          <%= link_to "Drivers", drivers_path %>
          <%= link_to "Add Driver", new_driver_path %>
          <%= link_to "Add Passenger", new_passenger_path %>
        </ul>
      </nav>
    </header>
  
    <section>
      <h1>Trip #:</h1>
      <ul>
          <li>ID: <%= @trip.id %></li> 
          <li>Driver: <%= @trip.driver_id %></li>
          <li>Passenger: <%= @trip.passenger_id %></li>
          <li>Date: <%= @trip.date %></li>
          <li>Rating: <%= @trip.rating %></li>
          <li>Cost: <%= @trip.cost %></li>
      </ul>
    </section>

    <section>
      <%= button_to 'Update Rating', trip_path(@trip.id), method: :put %>
    </section>

  </body>
</html>