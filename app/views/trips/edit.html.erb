<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>Edit Trip</title>
  </head>

  <body>
  <body>
    <header>
      <h1>Welcome to Rideshare</h1>
      <nav>
        <ul>
          <%= link_to "Rideshare Rails", root_path %>
          <%= link_to "Passengers", passengers_path %>
          <%= link_to "Drivers", drivers_path %>
          <%= link_to "Add Driver", new_driver_path %>
          <%= link_to "Add Passenger", new_passenger_path %>
        </ul>
      </nav>
    </header>

    <%= form_with model: @trip do |f| %>
      <p>Please provide the following information to update the passenger to our database:</p>

      <%= f.label :driver_id %>
      <%= f.text_field :driver_id  %>

      <%= f.label :passenger_id %>
      <%= f.text_field :pssenger_id  %>

      <%= f.label :rating %>
      <%= f.select :rating, [1..5].map{ |rating|} %>


      <%= f.label :cost %>
      <%= f.text_field :cost %>

      <%= f.label :date %>
      <%= f.text_field :date %>

    

      <%= f.label :phone_number, "Phone Number" %>
      <%= f.text_field :phone_number%>
      <%= f.submit "Update Trip" %>
    <% end %>
  </body>
</html>