<h2>
  <% if @passenger %>
    <%= @passenger.name %>'s
  <% elsif @driver %>
    <%= @driver.name %>'s
  <% else %>
    All
  <% end %>
Trips</h2>

<section>
<ul>
  <% @trips.order(date: :desc).each do |trip| %>
    <li>
      <%# Need to figure out method to display cost in dollars, not cents! %>
      <%= link_to trip.date, trip_path(trip.id) %>
    </li>
  <% end %>
</ul>

</section>
