<p>Please provide the following information to edit your trip:</p>

<%= form_with model: @trip, class: 'trip-form' do |f| %>
  <%= f.label :date %>
  <%= f.text_field :date %>

  <%= f.label :rating %>
  <%= f.text_field :rating %>

  <%= f.submit "Save changes to Trip", class: "trip-button" %>
<% end %>
